import { GamesDisplayPipe } from './games-display.pipe';

describe('GamesDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new GamesDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
